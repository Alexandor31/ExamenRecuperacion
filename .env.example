# Never commit real credentials. Copy this file to .env for local development.
# On Hugging Face Spaces set these values in Settings → Variables and secrets.

# LLM (OpenAI-compatible). Defaults to Groq (free tier).
LLM_API_KEY=
LLM_API_BASE=https://api.groq.com/openai/v1
LLM_MODEL=llama-3.3-70b-versatile

# Embeddings / re-ranking
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
ENABLE_RERANKER=true

# Retrieval
RETRIEVAL_CANDIDATES=20
TOP_K=5
MIN_SEMANTIC_SCORE=0.20
MIN_RERANK_SCORE=0.05

# Corpus processing
CHUNK_SIZE=1200
CHUNK_OVERLAP=200
MIN_CHUNK_CHARS=120
MAX_CONTEXT_CHARS=14000

# Persistence
DATA_DIR=./data
CHROMA_DIR=./data/chroma
CHROMA_COLLECTION=ir_corpus

# Behaviour
AUTO_BUILD_INDEX=false
MAX_DOCUMENTS=
LOG_LEVEL=INFO
